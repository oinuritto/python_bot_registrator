services:
  bot:
    build: .
    container_name: attendance-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Сохраняем БД и логи между перезапусками
      - ./data:/app/data
      - ./logs:/app/logs

