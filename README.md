# Attendance Bot

Telegram-бот для учёта посещаемости лекций. Позволяет преподавателям вести учёт посещаемости студентов по дисциплинам, просматривать статистику и экспортировать данные.

## Возможности

- **Управление дисциплинами** — создание, редактирование, удаление
- **Управление студентами** 
- **Отметка посещаемости** — интерактивный календарь, быстрые кнопки "Все присутствуют/отсутствуют"
- **Статистика** — по студенту, по дисциплине, общая; с фильтрацией по периоду
- **Визуализация** — графики посещаемости (по датам, по студентам)
- **Экспорт в Excel** — выгрузка данных с форматированием

## 🛠 Технологии

- **Python 3.12**
- **python-telegram-bot** — Telegram Bot API
- **SQLAlchemy** — ORM для работы с БД
- **SQLite** — база данных
- **Pandas** — анализ данных
- **Matplotlib / Seaborn** — визуализация
- **OpenPyXL** — экспорт в Excel
- **Pytest** — тестирование
- **Docker** — контейнеризация

## Схема базы данных

```
┌──────────────┐       ┌──────────────┐
│   Teacher    │──────<│   Subject    │
│──────────────│       │──────────────│
│ id           │       │ id           │
│ telegram_id  │       │ teacher_id   │
│ name         │       │ name         │
│ created_at   │       │ created_at   │
└──────────────┘       └──────┬───────┘
       │                      │
       │               ┌──────┴───────┐
       ▼               ▼              │
┌──────────────┐  ┌─────────────────┐ │
│   Student    │  │ SubjectStudent  │ │
│──────────────│  │─────────────────│ │
│ id           │  │ id              │ │
│ teacher_id   │──│ student_id      │ │
│ full_name    │  │ subject_id      │─┘
│ created_at   │  │ created_at      │
└──────────────┘  └────────┬────────┘
                           │
                           ▼
                  ┌──────────────────┐
                  │    Attendance    │
                  │──────────────────│
                  │ id               │
                  │ student_id       │
                  │ subject_id       │
                  │ date             │
                  │ is_present       │
                  │ created_at       │
                  └──────────────────┘
```

**Связи:**
- `Teacher` 1:N `Subject` — преподаватель имеет несколько дисциплин
- `Teacher` 1:N `Student` — преподаватель имеет свой пул студентов
- `Subject` N:M `Student` — через `SubjectStudent` (студент может быть на нескольких дисциплинах)
- `Attendance` — записи посещаемости (студент + дисциплина + дата)

## 🚀 Установка и запуск

### Локально

```bash
# Клонировать репозиторий
git clone https://github.com/your-username/attendance-bot.git
cd attendance-bot

# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
.\venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt

# Настроить переменные окружения
cp .env.example .env
# Отредактировать .env и добавить BOT_TOKEN

# Запустить бота
python -m bot.main
```

### Docker

```bash
# Настроить переменные окружения
cp .env.example .env
# Отредактировать .env и добавить BOT_TOKEN

# Собрать и запустить
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

## 📁 Структура проекта

```
attendance-bot/
├── bot/
│   ├── __init__.py
│   ├── main.py              # Точка входа
│   ├── config.py            # Конфигурация
│   ├── states.py            # Состояния
│   ├── database/
│   │   ├── __init__.py
│   │   ├── connection.py    # Подключение к БД
│   │   ├── models.py        # SQLAlchemy модели
│   │   └── crud.py          # CRUD операции
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── subjects.py      # Управление дисциплинами
│   │   ├── students.py      # Управление студентами
│   │   ├── subject_students.py  # Студенты в дисциплине
│   │   ├── attendance.py    # Отметка посещаемости
│   │   ├── export.py        # Экспорт данных
│   │   └── stats.py         # Статистика
│   └── utils/
│       ├── __init__.py
│       ├── calendar.py      # Интерактивный календарь
│       ├── charts.py        # Генерация графиков
│       ├── export.py        # Создание Excel файлов
│       └── stats.py         # Расчёт статистики
├── tests/
│   ├── __init__.py
│   ├── conftest.py          # Pytest фикстуры
│   ├── test_crud.py         # Тесты CRUD
│   └── test_stats.py        # Тесты статистики
├── data/                    # БД (не в git)
├── logs/                    # Логи (не в git)
├── .env.example             # Шаблон переменных окружения
├── .gitignore
├── Dockerfile
├── docker-compose.yml
├── pytest.ini
├── requirements.txt
└── README.md
```

## Тестирование

```bash
# Запуск всех тестов
python -m pytest tests/ -v
```

## Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram бота | — (обязательно) |
| `DATABASE_URL` | URL подключения к БД | `sqlite:///data/database.db` |
| `LOG_LEVEL` | Уровень логирования | `INFO` |
| `LOGS_DIR` | Директория для логов | `logs` |
